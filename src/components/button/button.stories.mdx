import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import { Button } from "../../src/components/button";

<Meta title="Components/Botão" comopnent="{Button}" />

# Tipografia

esta é a nossa tipografia

<ArgsTable of={Button} />

<Canvas>
  <Story
    name="Botão componente"
    args={{
      label: "Enviar",
      type: "type",
    }}
  >
    {(args) => <Button {...args} />}
  </Story>
</Canvas>
